{# {{ path('app_listado_tarea') }} #}
{# {{ path('app_crear_tarea_servicio') }} #}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_listado_tarea') }}"><i class="bi-calendar3 fa-5x"></i> TAREAS</a>
    {# <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> #}
    {# <div class="collapse navbar-collapse" id="navbarText"> #}
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        {# Un administrador también tendrá el rol de usuario por defecto #}
        {% if is_granted('ROLE_USER') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_tarea_new') }}">Crear</a>
          </li>
        {% endif %}

        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_user_index') }}">Listado de usuarios</a>
          </li>
        {% endif %}

        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout') }}">Cerrar sesión ({{ app.user.email }})</a>
          </li>
        {% endif %}

      </ul>
    {# </div> #}
  </div>
</nav>